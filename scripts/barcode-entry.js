// Bundled locally by setup-vision.mjs; no remote script runs on the sign-in page.
export { BrowserMultiFormatReader } from "@zxing/browser";
export { BarcodeFormat, DecodeHintType } from "@zxing/library";
